<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geeklub.ui"
    android:versionCode="1"
    android:versionName="1.0" >
    
    <!-- 测试数据库添加的 -->
    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="com.geeklub.ui" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <!-- 测试数据库添加的 -->
        
        <uses-library android:name="android.test.runner" />
        
        <activity android:name="com.geeklub.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <activity android:name="com.geeklub.ui.UserHome" android:label="我的应用"></activity>
        <activity android:name="com.geeklub.ui.Boutique" android:label="精品应用"></activity>
        <activity android:name="com.geeklub.ui.Ranking"  android:label="应用排行"></activity>
    </application>

</manifest>
